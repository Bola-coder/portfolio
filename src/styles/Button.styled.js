import styled from "styled-components";

export const Button = styled.button`
  padding: 20px 30px;
  font-size: 1.5rem;
  border: none;
  border-radius: 12px;
  color: #00008b;
  font-weight: bolder;
  margin-top: 20px;
  width: 250px;
  cursor: pointer;

  &:hover {
    opacity: 0.89;
    transform: scaleX(0.97);
  }
`;
